<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No permissions: tones are synthesized in-process. -->

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.X3BrainGames">

        <!-- RayNeo launcher visibility (the only hard vendor requirement). -->
        <meta-data
            android:name="com.rayneo.mercury.app"
            android:value="true" />

        <!-- No ar_mode meta-data: it restricts rendering to the left lens
             (FABLE_X3_STARTER_GUIDE gotcha #3). -->

        <!-- Split MAIN/LAUNCHER and MAIN/AR_APP filters + non-resizable:
             the launch-verified form from TapBubbles (guide gotcha #24 addendum). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="false"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustNothing"
            android:configChanges="density|orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|keyboard|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.rayneo.intent.category.AR_APP" />
            </intent-filter>
        </activity>

    </application>
</manifest>
